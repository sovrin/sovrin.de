services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: sovrin-de
    container_name: sovrin-de
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
    volumes:
      - ./content:/app/content:ro
