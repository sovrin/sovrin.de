{
  "name": "sovrin.de",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "clean": "lerna clean --yes",
    "backend:dev": "lerna run --stream --no-prefix --scope backend dev",
    "frontend:dev": "lerna run --stream --no-prefix --scope frontend dev"
  },
  "author": "",
  "license": "ISC"
}
